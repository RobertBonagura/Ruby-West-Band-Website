import React from 'react';
import Box from '@mui/material/Box';

const About = ({className}) => (
  <section id="about" className={className}>
    <Box>
      <h1>About Ruby West</h1>
      <p>We are a really cool band.</p>
    </Box>
  </section>
);

export default About;